// @flow
import {responsive} from './responsive';

const gridUnit = 10;

export const spacing = {
	xs: gridUnit * 0.5,
	sm: gridUnit * 0.75,
	base: gridUnit,
	lg: gridUnit * 1.25,
	xl: gridUnit * 1.5,
	xxl: gridUnit * 2,
};
